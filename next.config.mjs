/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'images.unsplash.com'], // Add the Unsplash domain here
      },
};

export default nextConfig;
